export OPTIMX_DEFAULT_PACKAGE={{server_name}}
gunicorn \
    --workers {{workers}} \
    -b :{{port}} \
    --preload \
    --worker-class=uvicorn.workers.UvicornWorker \
    'optimx.api:create_optimx_app()'